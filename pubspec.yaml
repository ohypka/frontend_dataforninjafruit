name: frontend_dataforninjafruit
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.2
  share_plus: ^12.0.1
  flutter_blue_plus: ^2.2.1
  permission_handler: ^12.0.1
  path_provider: ^2.1.3
  file_picker: ^8.0.7
  archive: ^3.6.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
